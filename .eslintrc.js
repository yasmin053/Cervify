module.exports = {
    env: {
      node: true, // Enable Node.js globals
      es2021: true // Optional modern JavaScript features
    },
    extends: [
      "eslint:recommended" // Use default recommended ESLint rules
    ],
    parserOptions: {
      ecmaVersion: 12 // Specify ECMAScript version
    },
    rules: {
      // You can add custom rules here (optional)
    }
  };
  